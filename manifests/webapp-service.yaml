apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  type: NodePort
  selector:
    app: webapp-sports
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30000
    protocol: TCP